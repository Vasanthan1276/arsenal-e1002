import fs from 'node:fs/promises';
const football = JSON.parse(await fs.readFile('data/football.json','utf8'));
const fantasy = JSON.parse(await fs.readFile('data/fantasy.json','utf8'));
const esc = (v='') => String(v ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
const fmt = v => v === null || v === undefined || v === '' ? '—' : Number.isFinite(Number(v)) ? Number(v).toLocaleString('en-SG') : esc(v);
const signed = v => Number(v)>0 ? `+${v}` : String(v ?? 0);

const standings = football.standings || [];
const arsenal = standings.find(t => String(t.team).toLowerCase()==='arsenal') || {};
const next = (football.fixtures || [])[0] || {};
const isHome = String(next.home || '').includes('ARSENAL');
const opponent = isHome ? next.away : next.home;

function badge(team) { return team.source === 'live' ? '<span class="badge live">LIVE DATA</span>' : '<span class="badge fallback">FALLBACK SQUAD</span>'; }
function summaryCard(team, key) {
  const rankLabel = team.overallRank != null ? 'Overall rank' : team.leagueRank != null ? 'League rank' : team.rank != null ? 'Rank' : 'Rank';
  const rankValue = team.overallRank ?? team.leagueRank ?? team.rank;
  return `<button class="team-card" data-tab="${key}"><div class="team-card-top"><div><small>${esc(team.format || '')}${team.scoring ? ` • ${esc(team.scoring)}`:''}</small><h3>${esc(team.name)}</h3></div>${badge(team)}</div><div class="metrics"><div><b>${fmt(team.gameweekPoints)}</b><span>GW points</span></div><div><b>${fmt(team.totalPoints)}</b><span>Total</span></div><div><b>${fmt(rankValue)}</b><span>${rankLabel}</span></div></div><p>${esc(team.status || '')}</p></button>`;
}
function playerTable(team) {
  const players = Array.isArray(team.players) ? team.players : [];
  if (!players.length) return '<div class="notice">No players loaded yet.</div>';
  const order = {GKP:1,DEF:2,MID:3,FWD:4};
  const sorted = [...players].sort((a,b)=>(a.starter===false)-(b.starter===false)||(order[a.position]||9)-(order[b.position]||9));
  return `<div class="player-table"><div class="player-head"><span>Player</span><span>Pos</span><span>Club</span><span>GW</span><span>Pts</span></div>${sorted.map(p=>{
    const role = p.captain ? '<em>C</em>' : p.viceCaptain ? '<em>VC</em>' : '';
    const bench = p.starter===false ? '<i>BENCH</i>' : '';
    return `<div class="player-row ${p.starter===false?'bench':''}"><span><b>${esc(p.name)}</b>${role}${bench}</span><span>${esc(p.position||'')}</span><span>${esc(p.club||'')}</span><span>${fmt(p.gwPoints)}</span><strong>${fmt(p.contribution ?? p.gwPoints)}</strong></div>`;
  }).join('')}</div>`;
}
function leagues(team) {
  const rows = team.leagues || [];
  if (!rows.length) return '<div class="notice">League details will appear when the Main FPL entry ID is configured.</div>';
  return `<div class="mini-table"><div class="mini-head"><span>League</span><span>Rank</span><span>Prev</span></div>${rows.map(l=>`<div><span>${esc(l.name)}</span><b>${fmt(l.rank)}</b><span>${fmt(l.lastRank)}</span></div>`).join('')}</div>`;
}
function draftStandings(team) {
  const rows = team.standings || [];
  if (!rows.length) return '<div class="notice">Draft standings will appear when leagueId + entryId are configured.</div>';
  return `<div class="mini-table"><div class="mini-head"><span>Manager</span><span>Rank</span><span>Points</span></div>${rows.map(r=>`<div><span>${esc(r.name)}</span><b>${fmt(r.rank)}</b><span>${fmt(r.points)}</span></div>`).join('')}</div>`;
}
function detailPanel(team,key,extra='') {
  return `<section class="tab-panel" id="tab-${key}"><div class="detail-title"><div><small>${esc(team.format || '')}${team.scoring?` • ${esc(team.scoring)}`:''}</small><h2>${esc(team.name)}</h2></div>${badge(team)}</div><div class="detail-metrics"><div><span>GW points</span><b>${fmt(team.gameweekPoints)}</b></div><div><span>Total points</span><b>${fmt(team.totalPoints)}</b></div><div><span>Rank</span><b>${fmt(team.overallRank ?? team.leagueRank ?? team.rank)}</b></div><div><span>Gameweek</span><b>${fmt(team.gameweek ?? fantasy.gameweek)}</b></div></div><div class="detail-grid"><div><h3>Players</h3>${playerTable(team)}</div><div><h3>${key==='main'?'Leagues':'League'}</h3>${key==='main'?leagues(team):key==='c1'?'<div class="notice">Challenge rank / league information will appear when the configured Challenge JSON source supplies it.</div>':draftStandings(team)}${extra}</div></div></section>`;
}

const main = fantasy.main || {name:'Main FPL',players:[]};
const d1 = fantasy.draft?.d1 || {name:'Golden Eagles D1',players:[]};
const d2 = fantasy.draft?.d2 || {name:'Golden Eagles D2',players:[]};
const c1 = fantasy.challenge?.c1 || {name:'Golden Eagles C1',players:[]};
const watch = (football.squadWatch || []).map(w=>`<div class="watch"><span class="watch-type ${String(w.type).toLowerCase()}">${esc(w.type)}</span><b>${esc(w.player)}</b><span>${esc(w.status)}</span></div>`).join('');
const fixtures = (football.fixtures || []).slice(0,4).map(f=>`<div class="fixture"><span>${esc(f.date)}</span><b>${esc(f.home)} <i>vs</i> ${esc(f.away)}</b><span>${esc(f.time)}</span></div>`).join('');
const table = standings.slice(0,10).map(t=>`<div class="pl-row ${String(t.team).toLowerCase()==='arsenal'?'arsenal':''}"><span>${fmt(t.pos)}</span><b>${esc(t.team)}</b><span>${fmt(t.played)}</span><span>${signed(t.gd)}</span><strong>${fmt(t.pts)}</strong></div>`).join('');
const form = (football.form || []).map(x=>`<span class="form ${x}">${esc(x)}</span>`).join('') || '<span>—</span>';

const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#d90012"><title>Arsenal + Fantasy Hub</title><style>
:root{--red:#d90012;--navy:#111827;--blue:#0067c5;--green:#00843d;--yellow:#ffd400;--bg:#f3f4f6;--card:#fff;--line:#d1d5db;--text:#111827;--muted:#6b7280}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:Inter,Arial,Helvetica,sans-serif}.shell{max-width:1500px;margin:auto;padding:18px}.hero{background:linear-gradient(135deg,var(--red),#92000c);color:white;border-radius:18px;padding:18px 22px;display:flex;justify-content:space-between;gap:20px;align-items:center;box-shadow:0 8px 24px #0002}.hero h1{margin:0;font-size:30px}.hero p{margin:4px 0 0;opacity:.9}.hero .next{text-align:right}.hero .next b{display:block;font-size:22px}.tabs{position:sticky;top:0;z-index:5;display:flex;gap:8px;overflow:auto;padding:12px 0;background:var(--bg)}.tab-btn{border:0;border-radius:999px;padding:9px 14px;font-weight:800;background:#fff;cursor:pointer;white-space:nowrap}.tab-btn.active{background:var(--navy);color:#fff}.overview-grid{display:grid;grid-template-columns:repeat(12,1fr);gap:14px}.card{background:var(--card);border-radius:16px;padding:16px;box-shadow:0 4px 16px #0001}.arsenal-card{grid-column:span 4}.fixtures-card{grid-column:span 5}.watch-card{grid-column:span 3}.fantasy-grid{grid-column:1/-1;display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.table-card{grid-column:span 5}.info-card{grid-column:span 7}.card h2,.card h3{margin:0 0 12px}.arsenal-big{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:16px}.arsenal-big div,.metrics div,.detail-metrics div{border:1px solid var(--line);border-radius:12px;padding:10px}.arsenal-big b{font-size:28px;color:var(--red);display:block}.arsenal-big span,.metrics span,.detail-metrics span{font-size:11px;color:var(--muted);font-weight:700}.form{display:inline-flex;width:28px;height:28px;align-items:center;justify-content:center;border-radius:8px;margin-right:5px;font-weight:900}.form.W{background:var(--green);color:#fff}.form.D{background:var(--yellow)}.form.L{background:var(--red);color:#fff}.fixture,.watch,.pl-row,.player-row,.player-head,.mini-table>div{display:grid;align-items:center;border-top:1px solid var(--line);min-height:36px;gap:8px}.fixture{grid-template-columns:72px 1fr 90px}.fixture span:last-child{text-align:right}.fixture i{font-style:normal;color:var(--muted);font-weight:500}.watch{grid-template-columns:82px 1fr 1fr}.watch-type{font-size:10px;font-weight:900;padding:4px 6px;border-radius:6px;width:max-content;background:var(--yellow)}.watch-type.transfer{background:var(--blue);color:#fff}.team-card{border:0;text-align:left;background:#fff;border-radius:16px;padding:16px;box-shadow:0 4px 16px #0001;cursor:pointer;color:inherit}.team-card:hover{outline:2px solid var(--red)}.team-card-top{display:flex;justify-content:space-between;gap:10px}.team-card h3{margin:2px 0 12px;font-size:18px}.team-card small,.detail-title small{color:var(--muted);font-weight:800}.badge{font-size:9px;font-weight:900;padding:5px 7px;border-radius:999px;height:max-content}.badge.live{background:#dcfce7;color:#166534}.badge.fallback{background:#fef3c7;color:#92400e}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.metrics b{display:block;font-size:22px}.team-card p{font-size:11px;color:var(--muted);margin:12px 0 0}.pl-head,.pl-row{grid-template-columns:35px 1fr 40px 50px 50px}.pl-head{display:grid;font-size:10px;font-weight:900;padding:7px 0;border-bottom:2px solid var(--navy)}.pl-row{padding:0 4px}.pl-row span:not(:nth-child(2)),.pl-row strong{text-align:center}.pl-row.arsenal{background:#fff0f1;color:var(--red);border:2px solid var(--red);border-radius:8px}.notice{background:#f9fafb;border:1px dashed var(--line);padding:12px;border-radius:10px;color:var(--muted);font-size:12px}.tab-panel{display:none;background:#fff;border-radius:18px;padding:18px;box-shadow:0 4px 16px #0001}.tab-panel.active{display:block}.detail-title{display:flex;justify-content:space-between;align-items:start}.detail-title h2{margin:2px 0 14px}.detail-metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:16px}.detail-metrics b{display:block;font-size:26px}.detail-grid{display:grid;grid-template-columns:2fr 1fr;gap:18px}.player-head,.player-row{grid-template-columns:1fr 55px 60px 50px 50px;padding:0 7px}.player-head{font-size:10px;font-weight:900;border-bottom:2px solid var(--navy)}.player-row{font-size:12px}.player-row strong,.player-row>span:nth-last-child(-n+3){text-align:center}.player-row.bench{opacity:.58}.player-row em,.player-row i{font-size:9px;font-style:normal;font-weight:900;padding:2px 4px;border-radius:4px;margin-left:5px}.player-row em{background:var(--red);color:#fff}.player-row i{background:#e5e7eb}.mini-table>div{grid-template-columns:1fr 70px 70px;padding:0 7px;font-size:12px}.mini-head{font-size:10px!important;font-weight:900;border-bottom:2px solid var(--navy)!important}.mini-table b,.mini-table span:nth-last-child(-n+2){text-align:center}.footnote{margin-top:12px;color:var(--muted);font-size:11px}.hidden{display:none}@media(max-width:1050px){.arsenal-card,.fixtures-card,.watch-card,.table-card,.info-card{grid-column:1/-1}.fantasy-grid{grid-template-columns:repeat(2,1fr)}.detail-grid{grid-template-columns:1fr}}@media(max-width:650px){.shell{padding:10px}.hero{border-radius:12px;align-items:flex-start}.hero h1{font-size:23px}.hero .next b{font-size:16px}.fantasy-grid{grid-template-columns:1fr}.detail-metrics{grid-template-columns:repeat(2,1fr)}.player-head,.player-row{grid-template-columns:1fr 42px 45px 42px}.player-head span:nth-child(3),.player-row span:nth-child(3){display:none}.watch{grid-template-columns:74px 1fr}.watch span:last-child{grid-column:2}.fixture{grid-template-columns:62px 1fr}.fixture span:last-child{grid-column:2;text-align:left}}</style></head><body><div class="shell">
<header class="hero"><div><h1>ARSENAL + FANTASY HUB</h1><p>Premier League • FPL • Draft • Challenge</p></div><div class="next"><small>NEXT MATCH</small><b>Arsenal ${isHome?'vs':'@'} ${esc(opponent || 'TBC')}</b><span>${esc(next.date || '')} • ${esc(next.time || '')}</span></div></header>
<nav class="tabs"><button class="tab-btn active" data-tab="overview">Overview</button><button class="tab-btn" data-tab="main">Main FPL</button><button class="tab-btn" data-tab="d1">D1 Draft</button><button class="tab-btn" data-tab="d2">D2 Draft</button><button class="tab-btn" data-tab="c1">C1 Challenge</button></nav>
<section class="tab-panel active" id="tab-overview"><div class="overview-grid">
<div class="card arsenal-card"><h2>Arsenal</h2><div>Form: ${form}</div><div class="arsenal-big"><div><b>${fmt(arsenal.pos)}</b><span>Position</span></div><div><b>${fmt(arsenal.pts)}</b><span>Points</span></div><div><b>${signed(arsenal.gd)}</b><span>Goal diff</span></div></div></div>
<div class="card fixtures-card"><h2>Fixtures</h2>${fixtures}</div><div class="card watch-card"><h2>Squad Watch</h2>${watch || '<div class="notice">No current alerts</div>'}</div>
<div class="fantasy-grid">${summaryCard(main,'main')}${summaryCard(d1,'d1')}${summaryCard(d2,'d2')}${summaryCard(c1,'c1')}</div>
<div class="card table-card"><h2>Premier League</h2><div class="pl-head"><span>#</span><span>Team</span><span>P</span><span>GD</span><span>Pts</span></div>${table}</div>
<div class="card info-card"><h2>Data status</h2><p><b>Football:</b> ${esc(football.updated || '—')}</p><p><b>Fantasy:</b> ${esc(fantasy.updated || '—')}</p><p><b>Fantasy gameweek:</b> ${fmt(fantasy.gameweek)}</p><div class="notice">The dashboard works with fallback squads now. Add your numeric FPL and Draft IDs in <b>config/fantasy-config.json</b> to replace fallback values with automatic live points and standings.</div></div>
</div></section>
${detailPanel(main,'main')}${detailPanel(d1,'d1')}${detailPanel(d2,'d2')}${detailPanel(c1,'c1','<div class="footnote">FPL Challenge uses the known squad as fallback unless a compatible JSON sourceUrl is configured.</div>')}
<p class="footnote">Generated from the same GitHub data pipeline as the E1002 dashboard. Last football update: ${esc(football.updated || '—')}.</p>
</div><script>const buttons=[...document.querySelectorAll('[data-tab]')];function show(tab){document.querySelectorAll('.tab-panel').forEach(p=>p.classList.toggle('active',p.id==='tab-'+tab));document.querySelectorAll('.tab-btn').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));window.scrollTo({top:0,behavior:'smooth'});}buttons.forEach(b=>b.addEventListener('click',()=>show(b.dataset.tab)));</script></body></html>`;
await fs.writeFile('ha.html',html,'utf8');
console.log('Generated Home Assistant dashboard: ha.html');
